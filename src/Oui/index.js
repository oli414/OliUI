import Window from "./Window";
import Box from "./Box";
import Label from "./Widgets/Label";
import Button from "./Widgets/Button";

const Oui = {
    Window: Window,
    Box: Box,
    Widgets: {
        Label: Label,
        Button: Button
    }
};

export default Oui;
